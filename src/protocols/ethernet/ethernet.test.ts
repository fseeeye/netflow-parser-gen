// import { Ethernet } from "./index"

const answer = ``

test('test ethernet', () => {
    // expect(
    //     `\n` + Ethernet.generateParser()
    // ).toEqual(answer)
    expect(``).toEqual(answer)
})