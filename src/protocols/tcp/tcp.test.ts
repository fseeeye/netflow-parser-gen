// import { Tcp } from "./index"

const answer = ``

test('test tcp', () => {
    // console.log(Tcp.generateParser())
    // expect(
    //     `\n` + Tcp.generateParser()
    // ).toEqual(answer)
    expect(``).toEqual(answer)
})