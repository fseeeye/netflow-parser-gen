export enum NomBytesFunction {
    take = 'take',
    tag = 'tag',
}

export enum NomMultiFunction {
    count = 'count',
}

export enum NomNumberFunction {
    u8 = 'u8',
    be_u16 = 'be_u16',
    be_u32 = 'be_u32',
    be_u64 = 'be_u64',
    le_u16 = 'le_u16',
    le_u32 = 'le_u32',
    le_u64 = 'le_u64',
}
